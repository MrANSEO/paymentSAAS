{
  "name": "payment-saas-fullstack",
  "version": "1.0.0",
  "description": "Paiement MeSomb fullstack (backend + frontend)",
  "main": "backend/server.js",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontendgui",
    "build": "npm run build --prefix frontendgui",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {}
}